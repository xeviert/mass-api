NODE_ENV='development'
PORT=8080
JWT_SECRET='change-me-in-production'
JWT_EXPIRY='3h'
# Optional: phone number that will be promoted to admin on startup if registered.
SEED_ADMIN_PHONE=''
# Optional: override the JSON data directory (defaults to ./data).
# DATA_DIR='./data'
