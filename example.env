NODE_ENV='development'
PORT=8080
JWT_SECRET='change-me-in-production'
JWT_EXPIRY='3h'
# Browser origin allowed to call the API in production.
CLIENT_ORIGIN='http://localhost:5173'
# Demo admin phone. The seed uses 5555550000 / demo by default.
SEED_ADMIN_PHONE='5555550000'
# Optional: override the JSON data directory (defaults to ./data).
# DATA_DIR='./data'
